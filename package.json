{
  "name": "array-control",
  "version": "1.0.1",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,cyan \"npm run dev -w @array/api\" \"npm run dev -w @array/web\"",
    "build": "npm run build -w @array/api && npm run build -w @array/web",
    "start": "npm run start -w @array/api",
    "check": "npm run check -w @array/api && npm run check -w @array/web",
    "test": "npm run test -w @array/api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=22"
  }
}
